# Отчет о деплое 2026-07-06

Дата проверки: 2026-07-06.

Ветка:

```text
feature/canonical-module-ownership
```

Документационный commit перед деплоем:

```text
0c0e7d2 Document canonical module architecture
```

## Что было развернуто

На SELKS установлен wheel:

```text
mikro_clear-0.1.0-py3-none-any.whl
```

Artifact был загружен в:

```text
/var/tmp/mikroclear-deploy/mikro_clear-0.1.0-py3-none-any.whl
```

Sha256:

```text
a884096972b3ddbe9a0fa446eea867b4cac2e5082d0277f0848762707b942e9b
```

Установка выполнена в:

```text
/opt/mikroclear-venv
```

## Production Service

Service:

```text
mikroclear.service
```

Active entrypoint:

```text
/opt/mikroclear-venv/bin/python -m mikroclear
```

Проверенное состояние после restart:

```text
ActiveState=active
SubState=running
NRestarts=0
```

Start time:

```text
Mon 2026-07-06 06:58:50 MSK
```

PID на момент проверки:

```text
4079492
```

## Import Checks На SELKS

Установленный package path:

```text
/opt/mikroclear-venv/lib/python3.11/site-packages/mikroclear/__init__.py
```

`app.build_service()` вернул:

```text
MikroClearService
```

`mikroclear.legacy_runtime` не найден:

```text
None
```

Ключевые файлы обновлены во время установки wheel:

```text
/opt/mikroclear-venv/lib/python3.11/site-packages/mikroclear/app.py
/opt/mikroclear-venv/lib/python3.11/site-packages/mikroclear/runtime/wiring.py
/opt/mikroclear-venv/lib/python3.11/site-packages/mikroclear/runtime/providers.py
```

Timestamp:

```text
2026-07-06 06:58:49 +0300
```

## Log Evidence

После restart service написал:

```text
[Mikro-Clear] Starting Mikro-Clear v3.1.1-TZSP0-ASSET-RESOLVER
[Mikro-Clear] Python: 3.11.2
[Mikro-Clear] RouterOS API: 192.168.10.1:8729 SSL
[Mikro-Clear] Telegram: enabled
[Mikro-Clear] Connected to MikroTik
[Mikro-Clear] Monitoring /opt/SELKS/docker/containers-data/suricata/logs/eve.json for Suricata alerts
```

В проверенном хвосте после текущего restart не было crash/traceback.

Исторические записи `status=226/NAMESPACE` относятся к 2026-07-05 и не относятся
к текущему deploy 2026-07-06.

## Локальные проверки перед deploy

```text
unittest discover: 188 tests OK
py_compile: OK
app.build_service(): MikroClearService
legacy_runtime import grep: empty
secret scan: empty
```

## Оставшиеся operational notes

- GitHub `gh auth status` ранее показывал невалидный token, но `git push`
  работал через configured remote.
- SELKS deploy package install требует sudo, потому что `/opt/mikroclear-venv`
  принадлежит `root:root`.
- Текущий целевой rollback - переустановка предыдущего wheel и restart
  `mikroclear.service`.
